// import { } from './constants/actions';

const initialState = {
    scores  : []
}

export default (state= initialState, action = {}) => {

    switch(action.type){

        default :
            return state;
    }
}